// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
    root: '.',          // Specifies the root directory for your project
    server: {
        port: 3000      // Specifies the port, e.g., http://localhost:3000
    }
});
